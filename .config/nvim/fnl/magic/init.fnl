(module magic.init)

(require :magic.globals)     ;; Globals we can access from all our modules
(require :magic.plugin)      ;; Here we list our plugins
(require :magic.options)     ;; Settings for the editor
(require :magic.keybindings) ;; Defines 99% of the mappings I want
