(module magic.init)

(include :magic.globals)     ;; Globals we can access from all our modules
(include :magic.options)     ;; Settings for the editor
(include :magic.keybindings) ;; Defines 99% of the mappings I want
(include :magic.plugin)      ;; Here we list our plugins
